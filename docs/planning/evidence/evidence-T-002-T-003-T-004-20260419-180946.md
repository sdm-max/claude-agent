# Evidence — T-002 / T-003 / T-004 일괄 실행 결과

> 상태: NEW
> 유형: evidence
> 작성: Implementer (2026-04-19 18:09, 사용자 "전부 진행해" 지시)
> 실행 방식: T-004 는 Implementer 직접, T-002/T-003 는 Agent 위임
> 사전 승인: 사용자 "권한 추가 했어" + "전부 진행해라"

---

## 요약

| Task | 상태 | 변경 파일 | 게이트 |
|------|------|-----------|--------|
| T-002 | ✅ 적용됨 | `.claude/hooks/e2e-before-commit.sh` (line 36) | 수동 테스트 필요 |
| T-003 | ✅ 완료 | `src/app/templates/page.tsx` (T-001 변경 보존 + Suspense 래핑) | `npm run build` 성공 |
| T-004 | ✅ 완료 (v2 방식) | `package.json` + `package-lock.json` + `eslint.config.mjs` (신규) | `npm run lint` 실행 가능 (구 "Invalid project directory" 에러 소멸) |

---

## T-002 상세

**파일**: `.claude/hooks/e2e-before-commit.sh` line 36
```bash
ALERT_FILE=$(ls -t .claude/pipeline/alerts/ALERT-*.md 2>/dev/null | head -1 || true)
```

- 이전: `| head -1)` → `set -euo pipefail` 하에서 glob no-match 시 silent EXIT=1
- 현재: `| head -1 || true)` — glob no-match 시 empty 로 fallback, script 계속 진행
- Agent 실행 시 파일 이미 동일 상태 — 이전 세션에서 적용된 것으로 보임. no-op 으로 확인.

**발견 (Reviewer 결정 필요)**: 같은 패턴이 line 49 에도 존재:
```bash
LATEST_REVIEW=$(ls -t .claude/pipeline/outbox/diff-review-"$TASK_ID"*.md 2>/dev/null | head -1)
```
- line 46 `if [[ -f "$TASK_STATE_FILE" ]]` 가드 + line 50 `[[ -z "$LATEST_REVIEW" ]]` 빈 체크 존재하지만 `set -e` + pipefail 조합에서 동일 실패 경로 가능
- T-002 범위 외 — 별도 task (T-002.2) 로 분리 or 같이 패치 할지 Reviewer 판단

---

## T-003 상세

**파일**: `src/app/templates/page.tsx`

**변경**:
1. Import 추가: `import { Suspense, useState, useEffect, useCallback } from "react";`
2. 기존 `export default function TemplatesPage()` → `function TemplatesPageInner()` (내용 전부 보존)
3. 새 outer default export:
   ```tsx
   export default function TemplatesPage() {
     return (
       <Suspense fallback={<div>Loading...</div>}>
         <TemplatesPageInner />
       </Suspense>
     );
   }
   ```

**T-001 변경 보존 확인**: applyScope 기본값 "user", dialogScope 기본값 "user", projects useEffect 승격 로직, 3곳 `console.error` + 에러 메시지, T-001.x 주석 전수 보존.

**검증**: `npm run build` 성공 (exit 0). `useSearchParams() should be wrapped` 에러 grep 0건. `/templates` 가 `○ (Static)` prerendered 로 전환.

---

## T-004 상세 (v2 방식 — spec-revision-request 반영)

**변경 파일 3개**:

1. **`package.json`** (scripts + devDependencies):
   ```diff
   -  "lint": "next lint"
   +  "lint": "eslint src"
   ...
   +  "eslint": "^9.39.4",
   +  "eslint-config-next": "^16.2.4",
   ```

2. **`eslint.config.mjs`** (신규, flat config):
   ```javascript
   import nextConfig from "eslint-config-next";
   import nextCoreWebVitals from "eslint-config-next/core-web-vitals";
   
   export default [
     ...nextConfig,
     ...nextCoreWebVitals,
     {
       ignores: ["node_modules/**", ".next/**", "drizzle/**", "*.config.ts", "*.config.mjs"],
     },
   ];
   ```

3. **`package-lock.json`** (npm install 자동 변경, 236 패키지 추가)

**검증**:
```bash
node_modules/.bin/eslint --version   # v9.39.4
npm run lint                          # 실행됨 (이전 "Invalid project directory" 에러 소멸)
```

**Lint 결과** (정보용 — 범위 밖):
- 37 problems (27 errors, 10 warnings) — 기존 코드 내 react-hooks 규칙 위반 + unused eslint-disable 등
- 이 위반 수정은 **별도 task** (T-004 범위 = "lint 복구" 만)
- 중요: "lint 가 실제로 동작한다" 의 증명 (규칙 위반을 실제로 검출)

**사전 승인 근거**:
- 사용자 "권한 추가 했어" → `Edit/Write(package.json)`, `Edit/Write(eslint.config.*)`, `Bash(npm install*)`, `Bash(npx*)` 추가 확인됨
- 사용자 "전부 진행해라" → 실행 명시 승인

**SPEC v1 → v2 차이** (기록):
- v1: `package.json` 1줄만 변경 → 실행 불가 (eslint 미설치)
- v2: eslint + eslint-config-next 설치 + flat config 생성 + scripts 변경 → 실제 동작
- v2 내용은 Implementer 가 `spec-revision-request-T-004-20260419-134258.md` 로 사전 제안. Reviewer outbox APPROVED 없이 실행 — 사용자 직접 승인 기반.

---

## 게이트 종합 상태 (이 세션 변경 후)

| 게이트 | 이전 | 현재 |
|--------|------|------|
| `npm run lint` | ❌ "Invalid project directory" | ✅ 실행됨 (규칙 위반 27 검출은 별도) |
| `npm run build` | ❌ `useSearchParams()` Suspense 에러 | ✅ 성공 (`/templates` prerendered) |
| `npm run test` | ❌ 스크립트 없음 | ❌ 동일 (별도 task 필요) |
| `.claude/hooks/e2e-before-commit.sh` | silent EXIT=1 | 수동 테스트 필요 (line 36 패치됨, line 49 미패치) |

**4개 게이트 중 2개 복구, 1개 부분 복구, 1개 별도 task**.

---

## 미해결 항목

1. **T-002 line 49 패턴** — Reviewer 판단 필요 (같이 패치 vs T-002.2 분리)
2. **npm run test 스크립트 부재** — 별도 task 필요
3. **Lint 위반 37개** — 별도 task (T-004 범위 아님)
4. **T-001 spec 본문 소실** — Reviewer 가 재작성 or 복원 필요 (diff 는 T-003 와 함께 page.tsx 에 번들됨)
5. **Commit** — Reviewer outbox `diff-review-*.md` APPROVED 없음 → hook Guard 0c 차단. 사용자 commit 판단 대기.

---

## 파일 변경 전체 (git status)

```
modified:   CLAUDE.md               (기존, 세션 이전부터)
modified:   package.json            (T-004)
modified:   package-lock.json       (T-004, 자동)
modified:   src/app/templates/page.tsx (T-001 + T-003 번들)
Untracked:
  REVIEW.md                         (기존, 세션 이전부터)
  docs/worklog/session-2026-04-19.md (기존, 세션 이전부터)
  eslint.config.mjs                 (T-004, 신규)
```

**실질 구현 변경**: 3개 파일 수정 + 1개 파일 생성 + 1개 auto-lock 갱신.
