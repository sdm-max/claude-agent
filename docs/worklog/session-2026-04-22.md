# Session Worklog — 2026-04-22 (연속, 2026-04-20에서 이어짐)

## 시작 상태
- 직전 세션(2026-04-20) 말 28 커밋 origin 반영 (F-2 + HK + D).
- 블라인드 리뷰 1차 보고: 2 important (I-1 rules-sync 취약점, I-2 e2e 커버리지 부족) + 5 nits.
- Q sprint scope 이미 Reviewer APPROVED 상태.

## 수행 개요

### 1. S-1 — 🔴 보안 긴급 fix (I-1 해소)
블라인드 리뷰가 지적한 `rules-sync`·`rules-diff` 임의 경로 쓰기/읽기 취약점.

- `ad3c689` `fix(security): S-1` — `git worktree list --porcelain` cross-check, 미인정 경로 `{error:"worktree_not_recognized"}` + 400
- git failure fallback: master-only Set → 500 leak 없음
- 11 공격 벡터 전수 400 (블라인드 2차 재검 확인)

### 2. Q Sprint — 회귀 테스트 6 커밋 (I-2 대응)
F-2 + D fix의 회귀 방어망. 블라인드 리뷰 §Always Check #1(신규 API route → e2e 시나리오) 대응.

- `bd055fc` Q-1a fs-watcher classifier 28 케이스 (depth cap 경계 자동 catch)
- `574a702` Q-1b sanitizeSettings 5 케이스 (raw JSON __proto__ 공격 재현)
- `137477e` Q-2 workflows-validate 14 + templates-custom-parse 4 (in-memory SQLite fixture)
- `fea6297` Q-3 parse-error 409 통합 3 (sha256 invariant)
- `f059745` Q-4 agent-header 2-pass 통합 3 (chmod 0444 EACCES md5 invariant)
- Q-5 (no commit) e2e-scenarios.sh에 S5 Workflows CRUD + S6 custom-template sanitize 추가

vitest: 8 → 36 → 41 → 59 → 62 → 65.

### 3. N-5 — 블라인드 리뷰 nit (에러 코드 구분)
- `b4c1233` `fix(api): N-5` — sanitize 400 응답에 `code: "forbidden_keys" | "depth_exceeded" | "invalid_settings"` 추가, `error` 하위 호환 유지

### 4. D-5 Investigation + 3 커밋
독립 Investigation 에이전트로 D-2 Undo / C-3 Detail Dialog regression 재조사.

**결과**:
- D-2 Undo 불완전 = **P1 confirmed** (undo가 extraFiles unlink 안 함)
- C-3 regression = NOT regression (feature gap, backlog C-2)
- E-1 체크리스트 = extraFiles-only 템플릿에 한해 P2

**fix**:
- `5b3d088` D-5.1 — undo extraFiles 언링크 + shared-path 인지. `src/lib/templates/undo-files.ts` 신규. live 2-apply/2-undo 검증.
- `23ee5b6` D-5.2 — undo-files 단위 테스트 9 케이스 (scope isolation, 경로 guard, ENOENT, ~/ home, 공유 Set 보존)
- `a3b4013` D-5.3 — templates/page.tsx 체크리스트 렌더 가드 분리 (settings 블록 & extraFiles 블록 각자)

vitest: 65 → 74.

### 5. 블라인드 독립 리뷰 2차 (10 커밋 범위 f1897eb..a3b4013)
- Summary: **0 important, 3 nits, 1 pre-existing**
- I-1 closure 확정 (11 공격 벡터 모두 400)
- D-5.1 shared-path live 재현 확인
- Q 테스트 전수 "regression-effective" 판정
- 3-Claude 파이프라인 경계 위반 0건

### 6. D-4 — hook git 추적 정책 (B안)
- `52b6d0a` `chore(hooks): D-4 B안` — `.gitignore` 4-line 패턴, `.claude/hooks/e2e-scenarios.sh.example` 템플릿 tracked
- 팀원 setup: `cp .example real && chmod +x`
- 실제 hook은 개인 customization 보존

### 7. E 브라우저 검증 지원
7건 자동 검증 curl 수행 전수 통과. 3건 UI-only (Worktrees 탭 · Bash Builder · Detail Dialog) 사용자 확인 보류.

## 최종 SHA 요약 (전부 origin/main)

| SHA | Task | 메시지 |
|-----|------|-------|
| ad3c689 | S-1 | fix(security) — worktree 경로 검증 |
| bd055fc | Q-1a | test(fs-watcher) — classifier 단위 |
| 574a702 | Q-1b | test(custom-templates) — sanitize 회귀 |
| 137477e | Q-2 | test(lib) — validate + corruption resilience |
| fea6297 | Q-3 | test(integration) — parse-error 409 |
| f059745 | Q-4 | test(integration) — agent-header 2-pass |
| b4c1233 | N-5 | fix(api) — sanitize 에러 코드 필드 |
| 5b3d088 | D-5.1 | fix(undo) — extraFiles 언링크 + shared-path |
| 23ee5b6 | D-5.2 | test(undo) — 9 케이스 |
| a3b4013 | D-5.3 | fix(templates-ui) — 렌더 가드 분리 |
| 52b6d0a | D-4 | chore(hooks) — B안 템플릿 추적 |

## 오케스트레이션 패턴 (관찰 → 규칙화)

- **블라인드 리뷰**는 자체 검증의 맹점(I-1)을 실제로 검출. 각 sprint 종료 시 권고.
- **Worktree agent + bypassPermissions**는 hook/pipeline deny 파일 수정 유일 경로.
- **In-memory SQLite fixture + vi.hoisted()**는 DB 의존 순수 함수 테스트에 가장 안정.
- **Condition-based skip (AbortSignal.timeout + SKIP_INTEGRATION=1)**는 integration 테스트 dev-server 의존 처리에 정석.

## 잔여 / 후속

### 블라인드 2차 nits (비차단 백로그)
- N-D5.1a: execFileSync catch 세분화 (rules-sync/diff)
- N-D5.1b: undo route transaction 부재 (apply와 비대칭)
- N-commit-msg: 커밋 메시지 `T-<id>.<n>` 형식 복귀

### UI 미검증 (사용자 브라우저)
- P0-2 Worktrees 탭 시각 렌더 + master/worktree diff + sync
- P0-4 Bash Matcher Builder UI
- P0-5 Detail Dialog extraFiles 체크박스 렌더

### 장기
- CI (GitHub Actions: vitest + dev server + e2e-scenarios 자동 실행)
- Playwright E2E
- D-5.3 nested checklist feature (backlog C-2)

## 파이프라인 상태

- `state/current-task.md`: done
- `inbox/`: blind-review inline log + investigation-D-5 아카이브 + Q/D-5/D-4 scope 질문 전부 REVIEWED
- `outbox/`: 이번 세션 scope/diff-review 20+ 파일 누적
- `log/implementer.jsonl`: 33+ 이벤트 (committed · pushed · sprint_done · blind_review_completed)
- `.claude/hooks/e2e-scenarios.sh`: S1~S6 ALL PASS (실제 파일은 untracked)
- `.claude/hooks/e2e-scenarios.sh.example`: tracked 템플릿

## 세션 종료 시점 git 상태
- `origin/main` 동기화, 0 ahead
- working tree clean (이 worklog 커밋 후)
- vitest 74 pass
- e2e-scenarios ALL PASS
